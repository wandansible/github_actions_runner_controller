---
arc_config_dir: /etc/github-actions-runner-controller
arc_manifests_dir: "{{ arc_config_dir }}/manifests"
arc_cert_manager_version: 1.8.0

arc_runners: []

arc_rootless_docker_image: docker:dind-rootless
